$(function(){ // Después de cargar DOM
    // Función deshabilitada temporalmente
    /*
    $(".cont-fondo-interno").addClass("cont-fondo-interno-animation");

    var fondos = $(".cont-fondo").find(".cont-fondo-interno");
    var fondoActual = Math.floor(Math.random() * (fondos.length - 0)) + 0;
    
    fondos.eq(fondoActual).fadeIn(500).siblings().fadeOut(500);

    setInterval(function(){
        fondoActual++;
        if(fondoActual == fondos.length){
            fondoActual = 0;
        }
        fondos.eq(fondoActual).fadeIn(500).siblings().fadeOut(500);
    }, 6000); // Debe estar la misma duración  que en el index.css
    */
});